enum ThemeMode {
  light,
  dark,
}

enum Gender { male, female }

enum SignInType { email, phone }

enum DocumentType {
  passport,
  titreDeSejour,
  carteIdentite,
}

enum HttpErrorType {
  connectionTimeout,
  socketException
}
